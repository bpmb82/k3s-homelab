apiVersion: v1
kind: ConfigMap
metadata:
  name: dind-docker-config
  namespace: forgejo-runner
data:
  daemon.json: |
    {
    "registry-creds": {
        "ghcr.io": {
        "auth": "{{ (ghcr_username ~ ':' ~ ghcr_token) | b64encode }}"
        }
      }
    }