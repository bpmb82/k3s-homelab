# Additional volumes on the output Deployment definition.
volumes:
  - name: {{ snzb_service }}-config
    persistentVolumeClaim:
      claimName: {{ snzb_service }}-config
  - name: {{ snzb_service }}-downloads
    persistentVolumeClaim:
      claimName: {{ snzb_service }}-downloads

# Additional volumeMounts on the output Deployment definition.
volumeMounts:
  - name: {{ snzb_service }}-config
    mountPath: /config
    readOnly: false
  - name: {{ snzb_service }}-downloads
    mountPath: /downloads
    readOnly: false
